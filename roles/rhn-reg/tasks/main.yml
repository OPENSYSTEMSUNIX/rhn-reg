---
# tasks file for rhn-reg
- name: RHEL7 | Register to RHN
  rhn_register:
    state: present
    username: "{{ username }}"
    password: "{{ password }}"
    server_url: "{{ server_url }}"
    activationkey: "{{ activationkey }}"